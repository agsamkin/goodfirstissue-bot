server:
#  port: ${PORT:8080}
port: 8080

spring:
  application:
    name: goodFirstIssueBot
    jpa:
      hibernate:
        ddl-auto: create-drop
        show_sql: true

  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml

logging:
  level:
    root: INFO